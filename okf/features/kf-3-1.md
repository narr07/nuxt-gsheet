---
type: Key Feature
title: "KF 3.1 — useGSheet(range, options?)"
description: "Core composable to fetch data from a specified range with caching and transformation options."
tags: [composables, fetching]
---

# KF 3.1 — `useGSheet(range, options?)`

Core composable to fetch range values asynchronously.

## Examples

```ts
// app.vue or any page
const { data, pending, error, refresh } = await useGSheet('Sheet1!A1:D10')

// With options
const { data } = await useGSheet('Sheet1!A:Z', {
  sheet: 'products',      // if multi-sheet config
  valueRenderOption: 'FORMATTED_VALUE',
  transform: (rows) => rows.map(([id, name, price]) => ({ id, name, price })),
  cache: true,
  cacheMaxAge: 60,        // seconds
})
```

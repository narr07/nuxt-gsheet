---
type: Key Feature
title: "KF 7.5 — Write via Apps Script (doPost)"
description: "Allows write operations to Google Sheets through POST requests to the Apps Script Web App."
tags: [public-sheet, apps-script, writing]
---

# KF 7.5 — Write via Apps Script (`doPost`)

Supports writing operations in `appscript` mode by extending the Apps Script logic.

## Google Apps Script Update
Add a `doPost` listener in the spreadsheet Apps Script editor:

```js
// Code.gs — add doPost
function doPost(e) {
  const body = JSON.parse(e.postData.contents)
  const ss = SpreadsheetApp.getActiveSpreadsheet()
  const sheet = ss.getSheetByName(body.sheet || 'Sheet1')

  if (body.action === 'append') {
    sheet.appendRow(body.row)
  } else if (body.action === 'update') {
    sheet.getRange(body.range).setValues(body.values)
  }

  return ContentService
    .createTextOutput(JSON.stringify({ success: true }))
    .setMimeType(ContentService.MimeType.JSON)
}
```

## Nuxt Usage Example
```ts
const { append, update } = useGSheetWrite()

// Append new row
await append({ sheet: 'Orders', row: ['ORD-001', 'Budi', '150000'] })

// Update range
await update({ sheet: 'Orders', range: 'B2', values: [['Nama Baru']] })
```

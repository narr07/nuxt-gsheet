---
type: Key Feature
title: "KF 7.2 — Mode: GViz JSON Feed (No Apps Script, No API Key)"
description: "Leverages Google's visualization endpoint to read public sheets without setup."
tags: [public-sheet, gviz, gql]
---

# KF 7.2 — Mode: GViz JSON Feed (No Apps Script, No API Key)

Google Visualization endpoint allows read-only access to sheets shared with "Anyone with link can view".

## Configuration Example
```ts
gsheet: {
  auth: 'gviz',
  spreadsheetId: process.env.GSHEET_SPREADSHEET_ID,
}
```

## Internal Parser Logic
The GViz API responds with a JSON wrapper that must be parsed:
```ts
// runtime/server/utils/gviz.ts
export async function fetchGviz(spreadsheetId: string, sheet?: string, query?: string) {
  const url = new URL(`https://docs.google.com/spreadsheets/d/${spreadsheetId}/gviz/tq`)
  url.searchParams.set('tqx', 'out:json')
  if (sheet) url.searchParams.set('sheet', sheet)
  if (query) url.searchParams.set('tq', query) // Google Query Language!

  const res = await fetch(url)
  const text = await res.text()

  // Response has a function wrapper like: google.visualization.Query.setResponse(...)
  const json = JSON.parse(text.replace(/^[^(]+\(|\);?$/g, ''))
  return parseGvizResponse(json)
}
```

## Google Query Language Support
Allows querying datasets using a SQL-like format:
```ts
const { data } = await useGSheet({
  sheet: 'Orders',
  query: 'SELECT A, B, C WHERE D > 100 ORDER BY B DESC LIMIT 10'
})
```

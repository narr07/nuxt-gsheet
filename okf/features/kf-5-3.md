---
type: Key Feature
title: "KF 5.3 — Cache invalidation"
description: "Supports manual cache invalidation via refreshing the composable state."
tags: [caching, invalidation]
---

# KF 5.3 — Cache invalidation

Manually invalidates and refreshes the cache.

## Examples

```ts
const { refresh } = await useGSheet('Sheet1!A:D')
// Trigger fresh fetch and updates cache storage
await refresh()
```
